import React from "react";
import '../styles/title.style.css'
const Title = ({ title }) => {
    return (
        <div className="seven">
            <h1>{title}</h1>
        </div>
    );
};

export default Title;
